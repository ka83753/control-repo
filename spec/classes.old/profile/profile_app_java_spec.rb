require 'spec_helper'

describe 'profile::app::java' do

    context "on #{os}" do
      let(:facts) do
        facts
      end
    end

    context "without any parameters" do
      it { is_expected.to compile.with_all_deps }
    end

end
